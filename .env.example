APP_NAME=NETRA-X
# Synthetic seed telemetry is explicitly labeled throughout the UI.
# Set false before using a fresh database for real telemetry only.
NETRA_SEED_DEMO=false
NETRA_DB_PATH=backend/data/netra.db
# Optional bearer token for API access.
NETRA_API_TOKEN=
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000


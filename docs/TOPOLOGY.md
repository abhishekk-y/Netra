# Topology Discovery

NETRA-X passive discovers the network topology by observing ARP, DHCP, and IP communication patterns.

- Devices are classified based on User-Agent strings and open ports.
- A graph structure is built to visualize the network in the UI.
